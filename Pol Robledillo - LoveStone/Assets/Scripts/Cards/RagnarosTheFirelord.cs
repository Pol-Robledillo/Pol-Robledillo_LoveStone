using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RagnarosTheFirelord : CharacterCard
{
    // Start is called before the first frame update
    void Start()
    {
        Invoke(null);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
