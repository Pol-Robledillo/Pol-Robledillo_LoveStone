using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TKRagnaros : Token
{
    // Start is called before the first frame update
    void Start()
    {
        SetToken();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
